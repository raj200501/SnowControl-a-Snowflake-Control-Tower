# Contributing

Thanks for contributing to FrostSight!

## Development setup
```bash
make bootstrap
```

## Checks
```bash
make verify
```

## Project conventions
- Python 3.11 only.
- Use `ruff` for backend linting.
- Keep tests deterministic (no network calls).
- Commit screenshots under `docs/screenshots/` when UI changes.
