# Contributing

Thanks for contributing to SnowControl!

## Development setup
```bash
make bootstrap
```

## Checks
```bash
make verify
```

## Project conventions
- Python 3.11 only.
- Use `ruff` for formatting and linting.
- Keep tests deterministic (no network calls).
